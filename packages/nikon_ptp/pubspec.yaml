name: nikon_ptp
description: Pure-Dart PTP / PTP-IP protocol implementation with Nikon Z vendor extensions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  collection: ^1.19.0
  meta: ^1.15.0

dev_dependencies:
  glados: ^1.1.6
  lints: ^5.1.0
  test: ^1.25.0
